# AGENTS.md

This is a personal GitHub profile repository (kaiquekandykoga/kaiquekandykoga). It contains only a static README.md profile page.

## No active project

- No build scripts, tests, or application code
- No package manager, no dependencies
- No AGENTS.md, CLAUDE.md, or other instruction files existed before this

## GitHub Actions

The `.github/workflows/opencode.yml` runs OpenCode on `/oc` or `/opencode` commands in issues/PRs.

## Commands

None - this is a static profile repository.
